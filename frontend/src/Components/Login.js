import React from "react";


const Login = () => {
 
  return (
    <div style = {{background:"var(--primary-color)", minHeight: "100vh"}}>
        Login
    </div>
      );
};

export default Login;
